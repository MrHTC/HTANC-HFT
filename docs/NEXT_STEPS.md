# NEXT STEPS

Follow the phased plan in `next_steps_action_plan.md` to complete paper testing and promotion gate.

Key immediate tasks:
- Fill `.env` with keys
- Install Docker Desktop
- Start `docker compose up --build`
- Run tests: `pytest -q`
